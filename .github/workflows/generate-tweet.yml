name: Generate tweet

on:
  schedule:
    - cron: '2 10 * * *'

  # Manually trigger this action
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.1.2'
        bundler-cache: true

    - run: bundle exec rake generate_tweet

    - name: Commit file to repo
      run: echo "going to commit, yes"
